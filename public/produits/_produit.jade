.product-item
    .col-4.produit-box
        p(class= produit.style) #{produit.price}$
        p.price-old #{produit.old}
        a(href= "/produits/#{ slug }")
            .produitshow(style= produit.imagebg)
        .produitindex
            p #{produit.headline}
